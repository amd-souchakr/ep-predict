# GPT-OSS Milestone C figures

Figure 1 shows the decisive instrumentation result. The ordinary router hook is bypassed on every MXFP4 layer, while the dispatch-boundary hook covers every layer and records zero expert-ID or selected-weight mismatches. This is a qualification result, not a routing distribution or performance measurement.
